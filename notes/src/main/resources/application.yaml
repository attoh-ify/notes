server:
  port: 9010

ws:
  allowed_origin: '${WS_ALLOWED_ORIGIN: ""}'

spring:
  datasource:
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
    username: ${PGUSER}
    password: ${PGPASSWORD}
